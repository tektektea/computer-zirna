import React from "react";
import Grid from "@material-ui/core/Grid";

const DownloadApp=props=>{
    return(
        <Grid container={true}>
            <Grid item={true}>
                <a href={''}>Download our android app</a>
            </Grid>
        </Grid>
    )
}
